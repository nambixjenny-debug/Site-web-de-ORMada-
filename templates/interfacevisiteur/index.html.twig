<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="ORMada - Organisation pour la gestion et la valorisation du patrimoine culturel de Madagascar">
    <meta name="keywords" content="patrimoine, Madagascar, culture, Rova, ORMada">
    <title>ORMada - Patrimoine Malagasy</title>
    
    {# Tailwind CSS #}
    <script src="https://cdn.tailwindcss.com"></script>
    
    {# Styles personnalisés #}
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .fade-in {
            animation: fadeIn 0.8s ease-out;
        }

        .hero-overlay {
            background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.7));
        }

        .hover-lift {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .hover-lift:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }

        html {
            scroll-behavior: smooth;
        }
    </style>
</head>

<body class="bg-gray-50 scroll-auto min-h-screen flex flex-col">

{# HEADER / NAVBAR #}
<header class="fixed top-0 left-0 w-full bg-green-600 shadow-lg z-50">
    <div class="max-w-7xl mx-auto flex justify-between items-center px-4 md:px-6 py-3">
        {# Logo et nom #}
       <div class="flex items-center gap-2 md:gap-3">
            <img src="{{ asset('images/Logo.jpg') }}"
                alt="Blason de Madagascar" 
                class="h-12 w-12 md:h-15 md:w-15 rounded-full object-cover">
            <span class="text-white font-bold text-lg md:text-xl">ORMada</span>
        </div>

        {# Navigation desktop #}
        <nav class="hidden md:flex gap-6 lg:gap-8 text-white font-semibold">
            <a href="#actualites" class="hover:text-yellow-300 transition duration-300">Actualité</a>
            <a href="#evenements" class="hover:text-yellow-300 transition duration-300">Événements</a>
            <a href="#informations" class="hover:text-yellow-300 transition duration-300">Infos</a>
            <a href="#apropos" class="hover:text-yellow-300 transition duration-300">À propos</a>
            <a href="#contacts" class="hover:text-yellow-300 transition duration-300">Contacts</a>
        </nav>

        {# Bouton menu mobile #}
        <button class="md:hidden text-white" onclick="toggleMobileMenu()" aria-label="Menu">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
        </button>
    </div>

    {# Menu mobile #}
    <div id="mobileMenu" class="hidden md:hidden bg-green-700 px-4 py-3">
        <nav class="flex flex-col gap-3 text-white font-semibold">
            <a href="#actualites" onclick="toggleMobileMenu()" class="py-2 hover:bg-green-600 px-3 rounded">Actualité</a>
            <a href="#evenements" onclick="toggleMobileMenu()" class="py-2 hover:bg-green-600 px-3 rounded">Événements</a>
            <a href="#informations" onclick="toggleMobileMenu()" class="py-2 hover:bg-green-600 px-3 rounded">Infos</a>
            <a href="#apropos" onclick="toggleMobileMenu()" class="py-2 hover:bg-green-600 px-3 rounded">À propos</a>
            <a href="#contacts" onclick="toggleMobileMenu()" class="py-2 hover:bg-green-600 px-3 rounded">Contacts</a>
        </nav>
    </div>
</header>

{# MAIN CONTENT #}
<main>

{# Affichage des messages flash #}
{% for type, messages in app.flashes %}
    {% if type == 'success' %}
    <div class="bg-green-50 border-l-4 border-green-600 p-4 m-4 rounded" role="alert">
        <div class="flex">
            <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-green-600" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                </svg>
            </div>
            <div class="ml-3">
                <p class="text-sm font-medium text-green-800">
                    {% for message in messages %}
                        {{ message }}{% if not loop.last %}<br>{% endif %}
                    {% endfor %}
                </p>
            </div>
        </div>
    </div>
    {% elseif type == 'error' %}
    <div class="bg-red-50 border-l-4 border-red-600 p-4 m-4 rounded" role="alert">
        <div class="flex">
            <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-600" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                </svg>
            </div>
            <div class="ml-3">
                <p class="text-sm font-medium text-red-800">
                    {% for message in messages %}
                        {{ message }}{% if not loop.last %}<br>{% endif %}
                    {% endfor %}
                </p>
            </div>
        </div>
    </div>
    {% endif %}
{% endfor %}

{# SECTION HERO #}
<section class="min-h-screen  bg-center flex items-center justify-center text-center px-4 mt-16" style="background-image:url('images/fondRova.jpg')">
    <div class="p-6 md:p-10 rounded-lg text-white w-full max-w-2xl fade-in">
        <p class="text-sm md:text-base mb-3 opacity-90">
            Pour la gestion et le développement durable du patrimoine de Madagascar
        </p>
        <h1 class="text-3xl md:text-5xl font-bold mb-6">
            Rovan' Madagasikara
        </h1>
        <div class="flex flex-col sm:flex-row justify-center gap-4">
            <a href="{{ path('app_histoire_du_rova') }}"
                class="px-6 py-2 bg-amber-600 hover:bg-amber-700 text-white rounded-lg transition">
                <span class="text-lg">🏰</span>
                <span class="font-medium">Histoire du Rova</span>

            </a>
     
            <button onclick="openModal()"
                    class="bg-green-600 hover:bg-green-700 px-6 py-2 rounded-lg transition duration-300 font-semibold">
                    Contactez-nous
            </button>
        </div>
    </div>
</section>

{# 
Partie actualités #}
<section id="actualites" class="py-20 bg-gray-100">

    <div class="max-w-6xl mx-auto px-4">

        <h2 class="text-4xl font-bold text-center mb-12 text-gray-800">
            Actualités
        </h2>

        <!-- CARROUSEL -->
        <div class="relative overflow-hidden rounded-2xl">

            <!-- Slides -->
            <div id="carousel" class="flex transition-transform duration-500 ease-in-out">

                {% for actualite in actualitesPublier %}

                    <!-- UNE SLIDE = UNE ACTUALITÉ -->
                    <div class="min-w-full px-1">

                        <div class="bg-white rounded-2xl shadow-md p-6 md:p-8">

                            <div class="grid md:grid-cols-2 gap-8 items-stretch">

                                <!-- ===================== -->
                                <!-- CONTENU -->
                                <!-- ===================== -->
                                <div class="flex flex-col justify-center">

                                    <span class="inline-block bg-blue-100 text-blue-700 text-xs font-semibold px-3 py-1 rounded-full mb-3 w-fit">
                                        Actualité
                                    </span>

                                    <h3 class="text-2xl md:text-3xl font-bold text-gray-800 mb-4">
                                        {{ actualite.titre }}
                                    </h3>

                                  
                                    {# <p class="text-gray-700 leading-relaxed text-justify">
                                        {{ actualite.contenu }}...
                                    </p> #}
                                    <p class="whitespace-pre-line leading-relaxed">
                                        {{ actualite.contenu }}
                                    </p>

                                </div>
                                <!-- ===================== -->
                                <!-- IMAGES -->
                                <!-- ===================== -->
                                <div class="grid md:grid-cols-1 gap-6 items-stretch">

                                    
                                    {# <div class="grid gap-3 h-full"> #}

                                        {% set totalImages = actualite.media|length %}

                                        {# ===================== #}
                                        {# 0 IMAGE #}
                                        {# ===================== #}
                                        {% if totalImages == 0 %}

                                            <img src="{{ asset('images/default.jpg') }}"
                                                class="w-full h-full min-h-[250px] object-cover rounded-xl shadow">

                                        {# ===================== #}
                                        {# 1 IMAGE #}
                                        {# ===================== #}
                                        {% elseif totalImages == 1 %}

                                            {% set image = actualite.media|first %}

                                            <img src="{{ asset(image.chemindufichier) }}"
                                                class="w-full h-full object-cover rounded-xl shadow hover:scale-105 transition duration-300">

                                        {# ===================== #}
                                        {# 2 IMAGES #}
                                        {# ===================== #}
                                        {% elseif totalImages == 2 %}

                                            <div class="grid grid-cols-2 gap-3 h-full">

                                                {% for image in actualite.media|slice(0,2) %}
                                                    <img src="{{ asset(image.chemindufichier) }}"
                                                        class="w-full h-full object-cover rounded-xl hover:scale-105 transition duration-300">
                                                {% endfor %}

                                            </div>

                                        {# ===================== #}
                                        {# 3+ IMAGES #}
                                        {# ===================== #}
                                        {% else %}

                                            <div class="grid grid-cols-2 gap-3 h-full">

                                                <!-- grande image -->
                                                <div class="row-span-2 overflow-hidden rounded-xl">
                                                    <img src="{{ asset(actualite.media[0].chemindufichier) }}"
                                                        class="w-full h-full object-cover hover:scale-105 transition duration-300">
                                                </div>

                                                <!-- 2e -->
                                                <img src="{{ asset(actualite.media[1].chemindufichier) }}"
                                                    class="w-full h-full object-cover rounded-xl hover:scale-105 transition duration-300">

                                                <!-- 3e + overlay -->
                                                <div class="relative overflow-hidden rounded-xl">

                                                    <img src="{{ asset(actualite.media[2].chemindufichier) }}"
                                                        class="w-full h-full object-cover hover:scale-105 transition duration-300">

                                                    {% if totalImages > 3 %}
                                                        <button
                                                            type="button"
                                                            onclick="openGallery('gallery{{ actualite.id }}')"
                                                            class="absolute inset-0 bg-black/60 text-white text-xl font-bold flex items-center justify-center">
                                                            +{{ totalImages - 3 }}
                                                        </button>
                                                    {% endif %}

                                                </div>

                                            </div>

                                        {% endif %}

                                     {# </div> #}

                                </div>

                            </div>

                        </div>

                    </div>

                {% else %}

                    <div class="min-w-full text-center py-20 bg-white rounded-2xl shadow">
                        <p class="text-gray-500 text-lg">
                            Aucune actualité disponible.
                        </p>
                    </div>

                {% endfor %}

            </div>

            

            <!-- CONTROLS -->
            <div class="flex flex-col items-center mt-8 space-y-4">

                <!-- Boutons -->
                <div class="flex gap-4">

                    <button onclick="prevSlide()"
                        class="w-10 h-10 rounded-full bg-white shadow hover:bg-gray-200 transition">
                        ‹
                    </button>

                    <button onclick="nextSlide()"
                        class="w-10 h-10 rounded-full bg-white shadow hover:bg-gray-200 transition">
                        ›
                    </button>

                </div>

                <!-- Dots dynamiques -->
                <div id="dots" class="flex gap-2">
                    {% for actualite in actualitesPublier %}
                        <span class="dot w-3 h-3 bg-gray-400 rounded-full cursor-pointer transition"></span>
                    {% endfor %}
                </div>

            </div>

        </div>

    </div>

</section>





{# SECTION ÉVÉNEMENTS #}
<section id="evenements" class="py-16 md:py-20">
    <div class="max-w-6xl mx-auto px-4">
        <h2 class="text-2xl md:text-3xl font-bold text-center mb-12 md:mb-16">
            Événements
        </h2>
        
        <div class="grid md:grid-cols-3 gap-6 md:gap-8">
            {# Carte 1 #}
            <div class="bg-white rounded-xl shadow-lg hover-lift">
                <img src="https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?w=600&auto=format"
                     alt="Événement culturel"
                     class="rounded-t-xl h-48 w-full object-cover">
                <div class="p-5">
                    <span class="text-sm text-green-600 font-semibold">15 Mars 2026</span>
                    <h3 class="font-bold text-xl mb-2 mt-1">Événement culturel</h3>
                    <p class="text-gray-600 mb-4">
                        Promotion des traditions culturelles malagasy.
                    </p>
                    <a href="#" class="text-green-600 font-semibold hover:underline">Voir détails →</a>
                </div>
            </div>
            
            {# Carte 2 #}
            <div class="bg-white rounded-xl shadow-lg hover-lift">
                <img src="https://images.unsplash.com/photo-1526779259212-939e64788e3c?w=600&auto=format"
                     alt="Visite du patrimoine"
                     class="rounded-t-xl h-48 w-full object-cover">
                <div class="p-5">
                    <span class="text-sm text-green-600 font-semibold">22 Mars 2026</span>
                    <h3 class="font-bold text-xl mb-2 mt-1">Visite du patrimoine</h3>
                    <p class="text-gray-600 mb-4">
                        Découverte des patrimoines historiques de Madagascar.
                    </p>
                    <a href="#" class="text-green-600 font-semibold hover:underline">Voir détails →</a>
                </div>
            </div>
            
            {# Carte 3 #}
            <div class="bg-white rounded-xl shadow-lg hover-lift">
                <img src="https://images.unsplash.com/photo-1472396961693-142e6e269027?w=600&auto=format"
                     alt="Activité communautaire"
                     class="rounded-t-xl h-48 w-full object-cover">
                <div class="p-5">
                    <span class="text-sm text-green-600 font-semibold">5 Avril 2026</span>
                    <h3 class="font-bold text-xl mb-2 mt-1">Activité communautaire</h3>
                    <p class="text-gray-600 mb-4">
                        Participation des habitants à la protection du patrimoine.
                    </p>
                    <a href="#" class="text-green-600 font-semibold hover:underline">Voir détails →</a>
                </div>
            </div>
        </div>
    </div>
</section>

{# SECTION INFORMATIONS #}
<section id="informations" class="py-16 md:py-20 bg-gray-100">
    <div class="max-w-6xl mx-auto px-4">
        <h2 class="text-2xl md:text-3xl font-bold text-center mb-12 md:mb-16">
            Informations
        </h2>
        
        <div class="grid md:grid-cols-2 gap-8 md:gap-12">
            <div class="bg-white p-6 rounded-xl shadow-md">
                <h3 class="text-red-600 font-bold text-xl mb-4">
                    Cultures et fady
                </h3>
                <p class="text-gray-700 leading-relaxed">
                    Le Rovan’i Madagasikara n’est pas seulement un site historique, 
                    mais aussi un lieu profondément ancré dans les traditions et les croyances malgaches.
                    Pour préserver son caractère sacré et respecter la mémoire des souverains, certaines règles culturelles, 
                    appelées fady, doivent être observées par tous les visiteurs
                </p>
                <button onclick="openFadyModal()"
                    class="bg-red-700 hover:bg-bleu-800 text-white px-6 py-3 rounded-lg shadow-lg transition mt-4">
                    En savoir plus...
                </button>
            </div>

            {# Modal qui affiche tous les fady liés au Rova de Manjakamiadana #}

        <div id="fadyModal"
            class="fixed inset-0 bg-black/60 hidden items-center justify-center z-50">

            <div class="bg-white rounded-xl shadow-2xl max-w-2xl w-full p-8 relative">

                <button onclick="closeFadyModal()" class="absolute top-3 right-3 text-gray-600 hover:text-red-600 text-xl">
                     ✖
                </button>

                <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center">
                    Les Fady du Rova d'Antananarivo
                </h2>

                <div class="flex gap-3 mb-[20px]">
                    <span class="text-red-600 text-xl">🚫</span>
                    <p>
                        Il est interdit de pointer du doigt les tombes royales ou les palais. 
                        Ce geste est considéré comme irrespectueux envers les ancêtres et les souverains 
                        qui reposent sur le site. Il est préférable d’indiquer avec la main ouverte par respect.
                    </p>
                </div>

                <div class="flex gap-3 mb-[20px]">
                    <span class="text-red-600 text-xl">📷</span>
                    <p>
                        La prise de photos peut être interdite dans certaines zones sacrées du Rova. 
                        Ces restrictions visent à préserver le caractère spirituel et la dignité des lieux. 
                        Il est recommandé de toujours demander l’autorisation avant de photographier.
                    </p>
                </div>

                <div class="flex gap-3 mb-[20px]">
                    <span class="text-red-600 text-xl">🕊</span>
                    <p>
                        Les tombes des rois et reines sont des lieux sacrés. Les visiteurs doivent éviter 
                        de marcher ou de s’asseoir à proximité de ces espaces et adopter une attitude respectueuse, 
                        car ils sont considérés comme des lieux de mémoire et de recueillement.
                    </p>
                </div>

                <div class="flex gap-3 mb-[20px]">
                    <span class="text-red-600 text-xl">🙏</span>
                    <p>
                        Un comportement respectueux est exigé sur l’ensemble du site. Cela inclut une tenue correcte, 
                        une attitude calme et le respect des consignes données par les guides ou les responsables du Rova.
                    </p>
                </div>

                <div class="flex gap-3 mb-[20px]">
                    <span class="text-red-600 text-xl">🍽</span>
                    <p>
                        Certains aliments peuvent être interdits selon les traditions locales (fady). 
                        Ces interdits varient selon les croyances et les coutumes. Il est conseillé 
                        de se renseigner auprès des guides pour éviter toute offense culturelle.
                    </p>
                </div>

                <div class="flex gap-3">
                    <span class="text-red-600 text-xl">🤫</span>
                    <p>
                        Le silence et le calme sont fortement recommandés dans les zones sacrées. 
                        Faire du bruit ou perturber l’environnement peut être perçu comme un manque de respect 
                        envers les ancêtres et l’importance spirituelle du lieu.
                    </p>
                </div>

                </div>
            </div>

{#      Inforamtion sur les tarifs d'entrée du Rova de Manjakamiadana #}
            
            <div class="max-w-4xl mx-auto">
    <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
        <!-- Bandeau décoratif vert émeraude -->
        <div class="h-2 bg-gradient-to-r from-emerald-600 to-emerald-400"></div>
        
        <div class="p-8">
            <!-- En-tête -->
            <div class="text-center mb-8">
                <div class="inline-block p-3 bg-emerald-100 rounded-full mb-4">
                    <svg class="w-8 h-8 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                </div>
                <h3 class="text-2xl font-bold text-gray-800 mb-2">
                    Tarifs d'entrée du Rova de Manjakamiadana
                </h3>
                <div class="w-20 h-1 bg-emerald-500 mx-auto rounded-full mb-4"></div>
                <p class="text-gray-600 leading-relaxed max-w-2xl mx-auto">
                    Les tarifs d'entrée varient en fonction de la nationalité, de l'âge et du type de visite (complète ou partielle). 
                    Des réductions sont disponibles pour les groupes de 5 personnes ou plus.
                </p>
            </div>

            <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">
                🎟️ Tarifs de visite
            </h2>

            <div class="space-y-6">

                <!-- VISITE COMPLETE -->
                <div class="border border-emerald-200 rounded-xl overflow-hidden">
                    <div class="bg-gradient-to-r from-emerald-600 to-emerald-500 px-6 py-3">
                        <h3 class="text-lg font-bold text-white">
                            🏚️ VISITE COMPLÈTE
                        </h3>
                    </div>

                    <div class="p-6 bg-white">
                        <div class="space-y-6">
                            <!-- Malagasy -->
                            <div class="border-b border-gray-100 pb-4 last:border-0">
                                <div class="flex items-center gap-2 mb-3">
                                    <span class="text-xl">🇲🇬</span>
                                    <p class="font-semibold text-gray-800">Malagasy et Résident</p>
                                </div>
                                <div class="space-y-2 ml-7">
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">Moins de 15 ans :</span>
                                        <div class="text-right">
                                            <span class="font-bold text-emerald-700 text-lg">5 000 Ar</span>
                                            <span class="text-gray-500 text-sm ml-2">(3 000 Ar si ≥ 5 pers)</span>
                                        </div>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">15 ans et plus :</span>
                                        <div class="text-right">
                                            <span class="font-bold text-emerald-700 text-lg">10 000 Ar</span>
                                            <span class="text-gray-500 text-sm ml-2">(7 000 Ar si ≥ 5 pers)</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Etranger -->
                            <div>
                                <div class="flex items-center gap-2 mb-3">
                                    <span class="text-xl">🌍</span>
                                    <p class="font-semibold text-gray-800">Étranger non résident</p>
                                </div>
                                <div class="space-y-2 ml-7">
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">Moins de 15 ans :</span>
                                        <div class="text-right">
                                            <span class="font-bold text-emerald-700 text-lg">20 000 Ar</span>
                                            <span class="text-gray-500 text-sm ml-2">(15 000 Ar si ≥ 5 pers)</span>
                                        </div>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">15 ans et plus :</span>
                                        <div class="text-right">
                                            <span class="font-bold text-emerald-700 text-lg">40 000 Ar</span>
                                            <span class="text-gray-500 text-sm ml-2">(30 000 Ar si ≥ 5 pers)</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- VISITE PARTIELLE -->
                <div class="border border-emerald-200 rounded-xl overflow-hidden">
                    <div class="bg-gradient-to-r from-emerald-600 to-emerald-500 px-6 py-3">
                        <h3 class="text-lg font-bold text-white">
                            👑 VISITE PARTIELLE <span class="text-sm font-normal">(Palais Manjakamiadana)</span>
                        </h3>
                    </div>

                    <div class="p-6 bg-white">
                        <div class="space-y-6">
                            <!-- Malagasy -->
                            <div class="border-b border-gray-100 pb-4 last:border-0">
                                <div class="flex items-center gap-2 mb-3">
                                    <span class="text-xl">🇲🇬</span>
                                    <p class="font-semibold text-gray-800">Malagasy et Résident</p>
                                </div>
                                <div class="space-y-2 ml-7">
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">Moins de 15 ans :</span>
                                        <div class="text-right">
                                            <span class="font-bold text-emerald-700 text-lg">2 000 Ar</span>
                                            <span class="text-gray-500 text-sm ml-2">(1 000 Ar si ≥ 5 pers)</span>
                                        </div>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">15 ans et plus :</span>
                                        <div class="text-right">
                                            <span class="font-bold text-emerald-700 text-lg">5 000 Ar</span>
                                            <span class="text-gray-500 text-sm ml-2">(3 500 Ar si ≥ 5 pers)</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Etranger -->
                            <div>
                                <div class="flex items-center gap-2 mb-3">
                                    <span class="text-xl">🌍</span>
                                    <p class="font-semibold text-gray-800">Étranger non résident</p>
                                </div>
                                <div class="space-y-2 ml-7">
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">Moins de 15 ans :</span>
                                        <div class="text-right">
                                            <span class="font-bold text-emerald-700 text-lg">10 000 Ar</span>
                                            <span class="text-gray-500 text-sm ml-2">(7 000 Ar si ≥ 5 pers)</span>
                                        </div>
                                    </div>
                                    <div class="flex justify-between items-center">
                                        <span class="text-gray-600">15 ans et plus :</span>
                                        <div class="text-right">
                                            <span class="font-bold text-emerald-700 text-lg">20 000 Ar</span>
                                            <span class="text-gray-500 text-sm ml-2">(15 000 Ar si ≥ 5 pers)</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            </div>

            <!-- Note de bas de page -->
            <div class="mt-6 pt-4 border-t border-gray-200 text-center">
                <div class="inline-flex items-center gap-2 text-sm text-gray-500">
                    <svg class="w-4 h-4 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <span>Réduction automatique pour les groupes de 5 personnes ou plus</span>
                </div>
            </div>
        </div>
    </div>
</div>
        </div>
</section>

{# SECTION À PROPOS #}
<section id="apropos" class="py-16 md:py-20">
    <div class="max-w-6xl mx-auto px-4">
        <h2 class="text-2xl md:text-3xl font-bold text-center mb-12 md:mb-16">
            À propos de nous
        </h2>
        
        <div class="grid md:grid-cols-3 gap-6 md:gap-8">
            <div class="bg-white shadow-lg rounded-xl p-6 text-center hover-lift">
                <h3 class="font-bold text-xl mb-3">Qui sommes-nous ?</h3>
                <p class="text-gray-600">
                    ORMada est une organisation dédiée à la gestion et à la valorisation 
                    du patrimoine culturel de Madagascar.
                </p>
            </div>
            
            <div class="bg-white shadow-lg rounded-xl p-6 text-center hover-lift">
                <h3 class="font-bold text-xl mb-3">Histoire de l'ORMada</h3>
                <p class="text-gray-600 mb-4">
                    L'organisation contribue à la préservation des sites historiques 
                    et culturels du pays.
                </p>
                <button class="bg-black text-white px-6 py-2 rounded-lg hover:bg-gray-800 transition">
                    En savoir plus
                </button>
            </div>
            
            <div class="bg-white shadow-lg rounded-xl p-6 text-center hover-lift">
                <h3 class="font-bold text-xl mb-3">Nos Missions et Visions</h3>
                <p class="text-gray-600">
                    Assurer la protection durable du patrimoine malagasy et sensibiliser 
                    les générations futures.
                </p>
            </div>
        </div>
    </div>
</section>

</main>

{# FOOTER #}
<footer id="contacts" class="bg-[#4b0000] text-white py-12 md:py-16">
    <div class="max-w-6xl mx-auto px-4">
        <div class="grid md:grid-cols-3 gap-8 md:gap-10">
            <div>
                <h3 class="text-green-400 font-bold text-lg mb-4">
                    Navigation rapide
                </h3>
                <ul class="space-y-2">
                    <li><a href="#" class="hover:text-green-400 transition">Accueil</a></li>
                    <li><a href="#apropos" class="hover:text-green-400 transition">À propos</a></li>
                    <li><a href="#actualites" class="hover:text-green-400 transition">Actualités</a></li>
                    <li><a href="#evenements" class="hover:text-green-400 transition">Événements</a></li>
                    <li><a href="#contacts" class="hover:text-green-400 transition">Contacts</a></li>
                </ul>
            </div>
            
            <div>
                <h3 class="text-blue-400 font-bold text-lg mb-4">
                    Informations de contact
                </h3>
                <p class="mb-2">Antananarivo, Madagascar</p>
                {% for parametre in parametres %} 
                    <p class="mb-2">{{ parametre.valeur }}</p>
                {% endfor %}
            </div>
            
            <div>
                <h3 class="font-bold text-lg mb-4">
                    Réseaux sociaux
                </h3>
                    <a href="https://www.facebook.com/p/Rovani-Madagasikara-100094032774345"
                        target="_blank"
                        class="flex items-center gap-2 text-blue-400 mb-3 hover:text-blue-500 transition">

                            <!-- Icône Facebook -->
                            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M22 12a10 10 0 10-11.5 9.9v-7h-2.2V12h2.2V9.8c0-2.2 1.3-3.4 3.3-3.4.9 0 1.8.1 1.8.1v2h-1c-1 0-1.3.6-1.3 1.2V12h2.3l-.4 2.9h-1.9v7A10 10 0 0022 12z"/>
                            </svg>

                            <span>Facebook</span>
                        </a>
                <button onclick="openModal()"
                        class="bg-green-600 hover:bg-green-700 px-6 py-3 rounded-lg transition duration-300 font-semibold">
                    Contactez-nous
                </button>
            </div>
        </div>
        
        <p class="text-center mt-10 pt-6 border-t border-red-800 text-sm">
            © 2026 ORMada. Tous droits réservés
        </p>
    </div>
</footer>

{# MODAL DE CONTACT #}
<div id="contactModal" 
     class="fixed inset-0 bg-black/60 hidden items-center justify-center z-50 p-4"
     onclick="if(event.target===this) closeModal()">
    
    <div class="bg-white rounded-xl p-6 md:p-8 w-full max-w-md max-h-106 overflow-y-auto">
        <h2 class="text-2xl text-center font-bold mb-6 text-gray-800">
            Contactez-nous
        </h2>
        
        <!-- Formulaire AJAX pour éviter les problèmes CSRF -->
        <form id="contactFormAjax" class="space-y-6">
            
            <!-- Champ NOM -->
            <div class="form-group">
                <label for="contact_nom" class="block text-sm font-semibold text-gray-700 mb-2">Nom</label>
                <input type="text" id="contact_nom" name="nom" placeholder="Votre nom" 
                       class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-green-500 focus:ring-2 focus:ring-green-200" 
                       required>
                <div class="text-red-500 text-xs mt-1 error-nom"></div>
            </div>

            <!-- Champ EMAIL -->
            <div class="form-group">
                <label for="contact_email" class="block text-sm font-semibold text-gray-700 mb-2">Email</label>
                <input type="email" id="contact_email" name="email" placeholder="votre.email@example.com" 
                       class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-green-500 focus:ring-2 focus:ring-green-200" 
                       required>
                <div class="text-red-500 text-xs mt-1 error-email"></div>
            </div>

            <!-- Champ TITRE -->
            <div class="form-group">
                <label for="contact_titre" class="block text-sm font-semibold text-gray-700 mb-2">Titre/Sujet</label>
                <input type="text" id="contact_titre" name="titre" placeholder="Sujet de votre message" 
                       class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-green-500 focus:ring-2 focus:ring-green-200" 
                       required>
                <div class="text-red-500 text-xs mt-1 error-titre"></div>
            </div>

            <!-- Champ DESCRIPTION -->
            <div class="form-group">
                <label for="contact_description" class="block text-sm font-semibold text-gray-700 mb-2">Message</label>
                <textarea id="contact_description" name="description" placeholder="Votre message" rows="4"
                          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-green-500 focus:ring-2 focus:ring-green-200" 
                          required></textarea>
                <div class="text-red-500 text-xs mt-1 error-description"></div>
            </div>

            <!-- Erreur générale -->
            <div id="contact_error" class="hidden bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded"></div>

            <!-- BOUTONS -->
            <div class="flex justify-between pt-6 border-t gap-3">
                <button type="button" onclick="closeModal()"
                   class="text-gray-600 hover:text-gray-900 text-sm px-5 py-2 rounded-md border border-gray-300 hover:bg-gray-100 transition">
                    ← Annuler
                </button>

                <button type="submit" id="submitBtn"
                    class="bg-green-600 hover:bg-green-700 text-white text-sm px-5 py-2 rounded-md transition">
                    Envoyer
                </button>
            </div>

        </form>
            
    </div>
</div>


{# SCRIPTS #}
<script>
// Gestion de la modal
function openModal() {
    const modal = document.getElementById('contactModal');
    modal.classList.remove('hidden');
    modal.classList.add('flex');
    document.body.style.overflow = 'hidden';
    resetContactForm();
}

function closeModal() {
    const modal = document.getElementById('contactModal');
    modal.classList.add('hidden');
    document.body.style.overflow = 'auto';
}

function resetContactForm() {
    const form = document.getElementById('contactFormAjax');
    if (form) {
        form.reset();
    }
    // Réinitialiser les messages d'erreur
    document.getElementById('contact_error').classList.add('hidden');
    document.querySelectorAll('[class^="error-"]').forEach(el => el.textContent = '');
}

// Soumission du formulaire de contact en AJAX
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('contactFormAjax');
    if (form) {
        form.addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const submitBtn = document.getElementById('submitBtn');
            const originalText = submitBtn.textContent;
            submitBtn.disabled = true;
            submitBtn.textContent = 'Envoi en cours...';
            
            // Récupérer les données du formulaire
            const formData = {
                nom: document.getElementById('contact_nom').value,
                email: document.getElementById('contact_email').value,
                titre: document.getElementById('contact_titre').value,
                description: document.getElementById('contact_description').value
            };

            try {
                const response = await fetch('{{ path("api_contact_submit") }}', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'X-Requested-With': 'XMLHttpRequest'
                    },
                    body: JSON.stringify(formData)
                });

                const result = await response.json();

                if (result.success) {
                    // Afficher le message de succès
                    showSuccessMessage('Votre message a bien été envoyé !');
                    resetContactForm();
                    setTimeout(() => closeModal(), 1500);
                } else {
                    showErrorMessage(result.error || 'Erreur lors de l\'envoi');
                }
            } catch (error) {
                showErrorMessage('Erreur réseau: ' + error.message);
            } finally {
                submitBtn.disabled = false;
                submitBtn.textContent = originalText;
            }
        });
    }
});

function showSuccessMessage(message) {
    const alertDiv = document.createElement('div');
    alertDiv.className = 'fixed top-4 right-4 bg-green-50 border-l-4 border-green-600 p-4 rounded shadow-lg z-50 max-w-sm';
    alertDiv.innerHTML = `
        <div class="flex">
            <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-green-600" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                </svg>
            </div>
            <div class="ml-3">
                <p class="text-sm font-medium text-green-800">${message}</p>
            </div>
        </div>
    `;
    document.body.appendChild(alertDiv);
    
    setTimeout(() => {
        alertDiv.remove();
    }, 3000);
}

function showErrorMessage(message) {
    const errorDiv = document.getElementById('contact_error');
    errorDiv.textContent = message;
    errorDiv.classList.remove('hidden');
}

// Fermer la modal avec la touche Echap
document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
        const modal = document.getElementById('contactModal');
        if (!modal.classList.contains('hidden')) {
            closeModal();
        }
    }
});

// Gestion du menu mobile
function toggleMobileMenu() {
    const menu = document.getElementById('mobileMenu');
    menu.classList.toggle('hidden');
}

document.addEventListener("DOMContentLoaded", function () {

    const carousel = document.getElementById("carousel");

    if (!carousel) return;

    const slides = carousel.children;
    const totalSlides = slides.length;

    if (totalSlides === 0) return;

    const dots = document.querySelectorAll("#dots .dot");

    let currentIndex = 0;
    let autoSlide;

    function updateCarousel() {
        carousel.style.transform = `translateX(-${currentIndex * 100}%)`;

        dots.forEach((dot, index) => {
            dot.classList.remove("bg-blue-600", "scale-125");
            dot.classList.add("bg-gray-400");

            if (index === currentIndex) {
                dot.classList.remove("bg-gray-400");
                dot.classList.add("bg-blue-600", "scale-125");
            }
        });
    }

    window.nextSlide = function () {
        currentIndex++;

        if (currentIndex >= totalSlides) {
            currentIndex = 0;
        }

        updateCarousel();
        restartAutoSlide();
    };

    window.prevSlide = function () {
        currentIndex--;

        if (currentIndex < 0) {
            currentIndex = totalSlides - 1;
        }

        updateCarousel();
        restartAutoSlide();
    };

    {# function startAutoSlide() {
        autoSlide = setInterval(() => {
            nextSlide();
        }, 5000);
    } #}

    function restartAutoSlide() {
        clearInterval(autoSlide);
        startAutoSlide();
    }

    dots.forEach((dot, index) => {
        dot.addEventListener("click", () => {
            currentIndex = index;
            updateCarousel();
            restartAutoSlide();
        });
    });

    let startX = 0;
    let endX = 0;

    carousel.addEventListener("touchstart", e => {
        startX = e.changedTouches[0].screenX;
    });

    carousel.addEventListener("touchend", e => {
        endX = e.changedTouches[0].screenX;

        if (startX - endX > 50) nextSlide();
        if (endX - startX > 50) prevSlide();
    });

    carousel.addEventListener("mouseenter", () => clearInterval(autoSlide));
    carousel.addEventListener("mouseleave", () => startAutoSlide());

    updateCarousel();
    startAutoSlide();

});



// Smooth scroll pour les ancres
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        }
    });
});


{# affichage du modal pour Fady #}
function openFadyModal(){
    document.getElementById("fadyModal").classList.remove("hidden");
    document.getElementById("fadyModal").classList.add("flex");
    }

function closeFadyModal(){
    document.getElementById("fadyModal").classList.remove("flex");
    document.getElementById("fadyModal").classList.add("hidden");
}



</script>

</body>
</html>