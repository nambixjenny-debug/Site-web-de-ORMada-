{{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}

<!-- Informations principales -->
<div>
    <h3 class="text-sm font-semibold text-gray-700 mb-2">
        Informations principales
    </h3>

    <div class="space-y-3">

        <!-- Titre -->
        <div>
            {{ form_label(form.titre, null, {'label_attr': {'class': 'block text-sm text-gray-700 mb-1'}}) }}

            {{ form_widget(form.titre, {
                'attr': {
                    'required': 'required',
                    'class': 'w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-green-500 focus:border-green-500'
                }
            }) }}

            <div class="text-red-500 text-xs mt-1">
                {{ form_errors(form.titre) }}
            </div>
        </div>

        <!-- Description -->
        <div>
            {{ form_label(form.description, null, {'label_attr': {'class': 'block text-sm text-gray-700 mb-1'}}) }}

            {{ form_widget(form.description, {
                'attr': {
                    'rows': '3',
                    'required': 'required',
                    'class': 'w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-green-500 focus:border-green-500'
                }
            }) }}

            <div class="text-red-500 text-xs mt-1">
                {{ form_errors(form.description) }}
            </div>
        </div>

    </div>
</div>


<!-- Dates -->
<div>
    <h3 class="text-sm font-semibold text-gray-700 mb-2">
        Dates
    </h3>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-3">

        <!-- Date début -->
        <div>
            {{ form_label(form.datedudebut, null, {'label_attr': {'class': 'block text-sm text-gray-700 mb-1'}}) }}

            {{ form_widget(form.datedudebut, {
                'attr': {
                    'required': 'required',
                    'class': 'w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-green-500 focus:border-green-500'
                }
            }) }}

            <div class="text-red-500 text-xs mt-1">
                {{ form_errors(form.datedudebut) }}
            </div>
        </div>

        <!-- Date fin -->
        <div>
            {{ form_label(form.datedufin, null, {'label_attr': {'class': 'block text-sm text-gray-700 mb-1'}}) }}

            {{ form_widget(form.datedufin, {
                'attr': {
                    'required': 'required',
                    'class': 'w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-green-500 focus:border-green-500'
                }
            }) }}

            <div class="text-red-500 text-xs mt-1">
                {{ form_errors(form.datedufin) }}
            </div>
        </div>

    </div>
</div>


<!-- Boutons -->


<div class="flex justify-between pt-4 border-t">

    <a href="{{ path('app_evenement_index') }}"
       class="text-gray-600 hover:text-gray-900 text-sm">
        ← Annuler
    </a>

    <button type="submit"
        class="bg-green-600 hover:bg-green-700 text-white text-sm px-5 py-2 rounded-md">
        {{ button_label|default('Créer') }}
    </button>

</div>

{{ form_end(form) }}